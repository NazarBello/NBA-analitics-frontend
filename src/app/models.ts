export interface TeamStanding {
  team: string;        // e.g. "LAL"
  wins: number;        // e.g. 50
  losses: number;      // e.g. 32
  winPct: number;      // e.g. 0.610
  gamesBehind: number; // e.g. 14.0
}
